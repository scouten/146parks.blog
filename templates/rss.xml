<?xml version="1.0" encoding="UTF-8"?>
<rss xmlns:atom="http://www.w3.org/2005/Atom" version="2.0">
    <channel>
      <title>{{ config.title }}
        {%- if term %} - {{ term.name }}
        {%- elif section.title %} - {{ section.title }}
        {%- endif -%}
      </title>
      <link>
        {%- if section -%}
          {{ section.permalink | escape_xml | safe }}
        {%- else -%}
          {{ config.base_url | escape_xml | safe }}
        {%- endif -%}
      </link>
      <description>{{ config.description }}</description>
      <generator>Zola</generator>
      <language>{{ lang }}</language>
      <atom:link href="{{ feed_url | safe }}" rel="self" type="application/rss+xml"/>
      <lastBuildDate>{{ last_updated | date(format="%a, %d %b %Y %H:%M:%S %z") }}</lastBuildDate>
      {%- for page in pages %}
      {%- if not page.extra.hide_from_section -%}
      <item>
          <title>{{ page.title }} {% if page.extra.designator %}{{ page.extra.designator }}{% else %}State Park{% endif %}</title>
          <pubDate>{{ page.date | date(format="%a, %d %b %Y %H:%M:%S %z") }}</pubDate>
          <author>
            {%- if page.authors -%}
              {{ page.authors[0] }}
            {%- elif config.author -%}
              {{ config.author }}
            {%- else -%}
              Eric Scouten
            {%- endif -%}
          </author>
          <link>{{ page.permalink | escape_xml | safe }}</link>
          <guid>{{ page.permalink | escape_xml | safe }}</guid>
          {%- if page.extra.cover_cdn_key -%}
          {%- set cover_url = "https://img.ericscouten.com/" ~ page.extra.cover_cdn_key ~ "/cover-" ~ page.extra.cover ~ "-1000w.jpg" -%}
          <description>&lt;img src="{{ cover_url }}" alt="{{ page.title }}" /&gt;{% if page.summary %}{{ page.summary }}{% else %}{{ page.content }}{% endif %}</description>
          {%- else -%}
          <description>{% if page.summary %}{{ page.summary }}{% else %}{{ page.content }}{% endif %}</description>
          {%- endif -%}
      </item>
      {%- endif -%}
      {%- endfor %}
    </channel>
</rss>
